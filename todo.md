# StampCamera TODO

## 완료
- [x] 빌드 에러/경고 정리 (CameraManager actor 격리, CGPoint Hashable)
- [x] 기기 설치용 Info.plist 필수 키(CFBundleIdentifier 등) 추가
- [x] stampFrame 누끼(배경+중앙창 제거) 후 카메라 정중앙 배치
- [x] 촬영 → 프레임 창 위치에 맞춰 우표(톱니) 모양으로 크롭
- [x] 앨범 대신 앱 내 컬렉션(디스크 영구 저장)에 저장
- [x] 촬영한 우표가 컬렉션 버튼으로 쓱 빨려드는 애니메이션
- [x] 컬렉션 그리드 화면(모은 우표 보기)
- [x] 진짜 우표처럼 크롭(촘촘한 perforation + 흰 종이 테두리)
- [x] 스탬프 기기 눌러서 촬영(눌림 → 찰칵 → 올라옴)
- [x] 애플식 줌: 배율 버튼(.5/1/2) + 0.5초 롱프레스 후 스와이프 조절
- [x] 스탬프 만들면 양피지에 우표 붙고 캡션(몇 마디) 작성·저장
- [x] 프레임 바깥 배경에 프로스티드 블러(윈도우만 선명) — 어지러움↓, 답답함 없음
- [x] 모으기/전시 공간 분리: 전시(액자 벽) 생성, 우표 꾹 눌러 전시에 걸기(컬렉션에서 빠짐), 드래그 재정렬, 되돌리기 — exhibitions.json 저장
- [x] 전시 배치 제스처화: 컬렉션 한 화면(우표첩+전시 두 구역), 흐름B(우표첩에서 우표 들어올려→전시 그리드로 드롭), 흐름A(전시 빈곳 탭→우표첩 우표 부채로 펼쳐 위로 끌어 걸기), 메뉴 폴백(상세/벽 셀)
- [x] 기능 명세서(SPEC.md) + Swift Testing 테스트 타깃(StampCameraTests) 추가 — 56개 테스트 전부 통과(CollectionStore/전시/StampShape/StampCompositor/Color)
- [x] 전시 다이어리 속지 배경 10종(절차 생성) — 전시 생성 시 선택(NewExhibitionSheet) + 나중에 변경, 벽/커버에 렌더, 저장
- [x] 촬영 토스 애니메이션 중력감 개선 — 스프링 낙하(감속) → 가속 자유낙하 + 2회 감쇠 바운스 + 임팩트 스쿼시, 햅틱을 첫 착지(0.64s)에 동기화
- [x] 촬영 직후 리빌 모던화(CaptureRevealView) — 양피지 시트 자동 오픈 대신 카드팩 개봉 스타일: 블러 위 홀로 카드(3D 틸트+무지개 광택+호버), 스파클 버스트, 인라인 캡션, "담기" 시 통으로 빨려들며 +1/바운스
- [x] 우표첩 스크롤 복구 — 셀의 LongPress+Drag sequenced 제스처가 터치를 점유해 스크롤 차단 → onLongPressGesture(들어올리기) + simultaneousGesture(운반)로 분리, 들어올리기 전엔 스크롤 우선
- [x] 리빌 카드 양면화 — 날짜·시간·장소를 앞면 텍스트 대신 뒷면 종이(실루엣)에 원형 소인으로, 가로 드래그로 자유 회전(플링+가까운 면 스냅, 90°에서 면 전환), 첫 회전 전 힌트 문구
- [x] 꾸미기(StampInk) — 상세페이지에 마감 6종(원본/세피아/흑백/빛바램/쨍하게/푸름) 미니 프리뷰 칩, 원본 사진에서 재합성(printStamp)이라 필터 전환 무손실, meta.filter로 선택 저장
- [x] 리빌에서 바로 버리기 — "버리기" 버튼: 카드가 화면 아래로 굴러떨어지며 collection.delete (통 +1 연출 없음)
- [x] 스탬핑 누름 연출 수정 — 본체·블러 펀칭이 줄며 고정 다이 안쪽에 뿌연 띠가 비치던 버그: 역할 반전(본체·펀칭 고정, 다이가 0.94+5pt로 눌림)
- [x] 인쇄 가이드 페이지(PrintGuideView) — 찍기→꾸미기→컬렉션→스티커 PDF→인쇄(집/인쇄소)→오리기 6단계 + 팁 박스, 진입점 2곳(모음 툴바 프린터 아이콘, 컬렉션 벽 ⋯ 메뉴)
- [x] 스티커 칼선 우표 모양으로 — 라운드 사각 kiss-cut → stampBezierPath 톱니 윤곽(흰 베이스도 같은 모양으로 블리드 확장, 칼선은 블리드 중간이라 톱니 둘레 흰 종이 띠)
- [x] 누름 역할 원복 — 파란 본체가 눌리고(0.9+8pt) 다이는 완전 고정, 블러 펀칭도 고정 유지(흰 띠 재발 없음)
- [x] 토스 낙하 낙엽화 — 정점에서 좌우 3회 살랑(스웨이+기울임+에지 틸트) 1초 하강, 바운스 없이 착지에 착 붙기(살짝 눌림+소프트 햅틱), 착지 1.30s
- [x] 낙하 속도 업 — 팔랑임 과해서 스윙 3→1.5회·하강 1.0→0.55s, 도착 속도 살려 착지 눌림 더 또렷하게(착지 0.85s)
- [x] 낙하 최종형 — 스윙 제거, 등가속 직하 0.25s + 착 눌림(착지 0.55s); 블러 펀칭 이중화(실루엣 펀칭은 눌림 따라가고 창 펀칭은 고정) — 누름 시 바깥 틈·창 안 뿌연 띠 둘 다 해소
- [x] 스티커 칼선 정합 — 톱니가 rect마다 재계산돼 아트/베이스/칼선 어긋나고 칼선이 사진 침범 → 아트 패스 하나를 중심 기준 기하 확대로 파생(완벽 동위상 네스팅), 블리드 2mm, /tmp/sticker-check.png로 시각 검증
- [x] 우표첩 수집 재미 — 수집가 헤더(카운트·장소 수·이번 달 + 마일스톤 진행바·달성 축하), 골드 에디션(10장마다 금박 매트+왕관+금빛 그림자), NEW 배지(24h)·첫 장소 핀 배지, 최신 달 빈 포켓(점선+plus), 포켓 캐스케이드 등장
- [x] 컬렉션 상세 책자화 — PageCurl → TabView 좌우 스와이프, 자유 배치 제거하고 항상 3×4(12장/페이지), 우표 제스처를 onLongPress+simultaneous로 바꿔 스와이프 차단 해소, 운반 중 scrollDisabled
- [x] 우표첩도 책자화 — 세로 스크롤·월 섹션 제거, 수집가 헤더 고정 + TabView 좌우 스와이프 3×4(12장/페이지), 오래된 순(1페이지=첫 수집)이라 새 우표가 빈 포켓 있는 마지막 장에 꽂힘, 열면 마지막 장에서 시작, 페이지 푸터 "— n / m —"
- [x] 우표첩 책 마무리 — 수집가 명판 당분간 숨김(코드는 보존), 컬렉션과 동일한 풀블리드 레이아웃(종이 전면 배경, 셀 크기 컬렉션과 같은 (W-68)/3, 하단 안전영역 무시)
- [x] 촬영 화질 업 — 스틸을 비디오 프레임 그랩 → AVCapturePhotoOutput 풀해상도(12MP, maxPhotoDimensions+quality 우선순위, 시스템 셔터음 감수)로 전환; 비디오 스트림은 프리뷰+라이브 버스트+실패 폴백 유지
- [x] 우표/스티커 개념 정리 — 우표=수집 본체, 스티커=쓰는 형태(출구): 컬렉션 메뉴에 "우표 쓰기" 섹션(이미지 공유/인쇄용 스티커 시트 PDF/인쇄 가이드), 상세 "스티커로 저장"→"우표 내보내기 (PNG)", 인쇄 가이드에 iMessage 자동 스티커 팁 추가
- [x] 우표첩에서 라이브 우표 재생 — 포켓 아트를 PocketArt로 교체: 라이브 클립 있으면 썸네일 크기(280px)로 1회 디코딩해 루프 재생(페이지당 12개여도 메모리 안전), 없으면 기존 정지 이미지
- [x] 라이브 재생 전면 확대 + 설정 — 공용 LiveArt로 통합(우표첩 280px·컬렉션 벽 360px·상세/퍼즐 풀해상도·리빌 카드 700px, 늦게 인코딩된 클립 자동 픽업), 카메라 라이브 메뉴에 "움직임 자동 재생" 토글(liveAutoPlay, 끄면 어디서나 정지)
- [x] iMessage 스티커 500KB 예산 — 풀사이즈 APNG를 그대로 트레이에 쓰면 초과 시 스티커가 서랍에서 사라지는 문제: 메시지 전용 경량 APNG(프레임 절반·300→240→190px 단계 축소·480KB 컷, 실패 시 정지 스티커 유지) 별도 인코딩
- [x] 라이브 클립 유실 레이스 수정 — 고화질 사진 전환으로 우표 등록(사진도착+0.55s)이 버스트(1.2s)보다 늦어질 수 있음: 버스트가 먼저 오면 guard로 버려지던 것을 ① 지오메트리를 셔터 시점에 확보 ② pendingLive 버퍼링 후 등록 시 부착으로 수정 + 썸네일 디코딩을 풀디코드+수동 축소로 교체(APNG 인덱스 신뢰성)
- [x] 촬영 직후 즉시 움직임 — 핫 클립 경로: 크롭 완료 즉시 디코딩된 프레임을 메모리로 UI에 전달(hotClips, APNG 인코딩 비대기), LiveArt가 핫 클립 우선 사용, 크롭을 userInitiated로 승격 — 리빌 카드 등장과 거의 동시에 재생 시작
- [x] 진짜 Live Photo 캡처 — isLivePhotoCaptureEnabled + livePhotoMovieFileURL: 시스템 롤링 버퍼로 셔터 전 순간까지 포함된 ~3초 무비 → AVAssetImageGenerator로 14프레임 추출(720px, 트랙 변환 적용) → 기존 파이프라인 합류; 딜레이를 실제 길이 기반으로(capturedLiveSeconds), 미지원 기기는 포스트 셔터 버스트 폴백
- [x] 클립을 찍은 순간 중심으로 — photoDisplayTime(무비 속 스틸 시점) 기준 ±0.75s 창으로 14프레임 추출(가장자리 클램프 시 1.5s 보장) — 스티커가 "딱 찍은 그 모습"으로 움직임
- [x] 리빌 카드: 라이브 우표는 호버 끄기 — 클립이 있으면(핫/디스크) 둥둥 모션 0, 자기 움직임만 재생(정지 우표는 기존 호버 유지)
- [x] 라이브 캡처 이중화(신뢰성 재설계) — Live Photo 무비가 유일 경로일 때 실패하면 모션 전무: 버스트를 항상 가동(링버퍼 프리롤 ~0.7s + 포스트 1.2s = 누르기 전 순간 포함 자체 구현), 무비는 도착하면 클립 교체하는 보너스로 강등; 재생도 UIImageView→TimelineView로 교체, 파이프라인 자동 테스트 6종(LiveStampTests) 추가
- [x] 라이브 최종 단순화 — Live Photo 세션 모드 완전 제거(비디오 출력과 조합이 현장에서 불안정), 검증된 표준 구성(사진 출력+비디오 출력)으로 회귀: 모션은 링버퍼 프리롤+버스트 단일 경로; 가위(오려내기) 모드가 라이브를 조용히 끄는데 아이콘이 노란불이던 정직성 버그 수정; DEBUG 빌드 리빌 카드에 클립 상태 진단 표시(clip: none/hot nf/disk)
- [x] 움직이는 배경제거 스티커(사진 앱 스타일) — 가위 모드에서도 라이브 허용: 클립 프레임마다 SubjectLift(Vision 피사체 마스크)로 배경 제거(~10프레임으로 솎고 딜레이 보정), 리빌 카드 마스크를 광택에만 적용(움직이는 피사체가 스틸 실루엣에 잘리지 않게)
- [x] 정지 스티커 화질 업 + 동기화 버그 — 공유 스티커를 화면용(~580px) 대신 원본에서 1024→800→640px 예산 사다리로 재생성(.v2-hires 마커로 기존 스티커 일괄 재생성), 라이브 동기화가 풀사이즈 APNG를 그대로 써서 500KB 초과 시 서랍에서 사라지던 버그도 경량 재인코딩으로 수정; 움직이는 스티커는 Messages 500KB 하드캡이 상한
- [x] 정체성 정제: 모습(StampStyle) 시스템 — 캡처는 항상 "라이브로 찍고 우표 모양으로 뜯기"(카메라 가위 토글·컷아웃 분기 제거), 무가공 모션 마스터(.frames/, jpeg 85%)를 영구 보관해 우표/라이브 우표/스티커/라이브 스티커 4모습을 언제든 무손실 전환(applyStyle: 스틸 재생성+클립 재파생+스티커 동기화+liveRev로 화면 즉시 갱신), 선택 UI는 리빌 카드+상세 페이지 공용 StampStylePicker, 모션 없는 우표는 라이브 모습 비활성; 테스트 2종 추가(마스터 왕복, 스타일 전환→클립 생성/제거, 총 64개)
- [x] 용어 정리 — "우표첩" → "수집함" 전면 교체(자동 수집 = 수집함, 큐레이션 = 컬렉션), 새 기본 수집함 이름 "내 수집함"
- [x] 모습 선택을 찍기 전으로 — 카메라 좌측 상단 버튼(현재 모습 아이콘)으로 4모습 중 선택(@AppStorage captureStyle, 기본 라이브 우표), 셔터가 그 모습 그대로 생산(스티커 모습은 피사체 리프팅 스틸, 라이브 모습만 클립 파생, 정지 모습도 마스터는 보관해 나중에 전환 가능), 리빌 카드 피커 제거(변경은 상세 페이지)
- [x] 카메라 정리 — 라이브포토 심볼 중복 해소: 설정 메뉴 버튼(라이브 우표 담기/움직임 자동 재생)을 카메라에서 빼고 모음 화면 툴바 기어(설정) 메뉴로 이동
- [x] 선택 삭제 수리 — PageCurl 책의 contentKey에 선택 상태가 빠져 보이는 페이지가 안 갱신되던 근본 원인 수정(sel/ticks를 키에 포함) + 하단 바에 "모두 선택/해제" 추가
- [x] 컬렉션 = 메시지 스티커 팩 — syncStickers가 collections.json(이름+파일 목록) 매니페스트 내보내고 saveExhibitions마다 갱신, 익스텐션 상단에 칩 바(전체+컬렉션별)로 필터링(큐레이션 순서 유지)
- [x] 라이브 컨트롤 충돌 해소 — 모습=라이브인데 기어 토글이 꺼져 있으면 조용히 정지로 찍히던 모순: 라이브 모습은 무조건 움직임 기록(captureStyle.needsMotion || liveStamps), 토글은 "움직임 항상 보관"(정지 모습용 마스터 보관)으로 의미 명확화
- [x] 포켓 배지 정리 — NEW는 24h 내 + 책에서 한 번 누르면 소멸(seenIDs 영구 기록, markSeen), 움직이는 우표엔 라이브 심볼(좌하단 노란 livephoto); 배지 상태를 leafContentKey에 포함해 컬 페이지 즉시 갱신
- [x] 옛 우표 움직임 손실 버그 수리 — 마스터 없는(모습 시스템 이전) 우표를 정지 모습으로 바꾸면 유일한 클립이 삭제돼 복구 불가였음: applyStyle이 어떤 전환이든 먼저 클립→마스터 구조 백필(motionMaterial), 라이브 칩 활성 조건도 canMove(마스터∨클립∨핫)로 확장, 구조 테스트 추가(총 65개); 이미 클립이 지워진 우표는 원천 부재로 복구 불가
- [x] 라이브 심볼 데이터 유효성 — 파일 존재가 아니라 실제 재생 가능(APNG 프레임 2+, 헤더만 파싱+리비전 캐시)으로 판정(isPlayablyLive), setLive/removeLive가 리비전 범프해 캐시 무결성 보장, 자동 재생 꺼짐 동안에도 심볼 숨김, 유효성 테스트 추가(총 66개)
- [x] 카메라 절전 — 모음 화면이 열려 있는 동안 세션 정지(초록불 꺼짐·배터리 절약), 닫으면 즉시 재가동(start/stop에 isRunning 가드)
- [x] 컬렉션 = 참조(플레이리스트) — 컬렉션에 걸어도 수집함에서 빠지지 않게(collectedStamps/stamps(in:)/count(in:)에서 전시 제외 제거), 단일 벽 멤버십·내리기 유지, 테스트 시맨틱 갱신
- [x] 수집함 모아보기 필터 — 툴바 깔때기 메뉴: 전체/움직이는 우표/우표 모습/스티커 모습/컬렉션에 건 우표, 필터별 빈 상태 안내, 빈 포켓은 전체에서만, contentKey 연동
- [x] 메시지 스티커 이름·크기 — 익스텐션 표시명/스티커 설명 "우표"→"컬렉션"; 작게 보이던 원인=Messages가 픽셀÷3로 렌더하는데 경량 인코더가 190~300px까지 축소(63~100pt): 크기 우선 사다리(480·408px×6f → 360×5 → 320·280×4, 프레임을 희생)로 교체해 보통 136~160pt로 표시, .v3-bigstickers 마커로 기존 스티커 일괄 재생성
- [x] 브랜딩 "펀치(PUNCH)" — 홈 화면 앱 이름(CFBundleDisplayName=펀치), 권한 화면 "📮 펀치", SNS 카드 로고 PUNCH(tracking 4), 우표 뒷면 레터링 PUNCH, 공유 해시태그 #펀치, README 타이틀+태그라인("세상에서 갖고 싶은 순간을 뜯어 모은다")
- [x] 지원·개인정보 페이지 — GitHub Pages HTML(docs/index·support·privacy.html, 스탬프 디자인: 크림 종이+점선 톱니+금색), Pages 활성화(dev /docs → m1zz.github.io/StampCamera), README는 라이브 URL로 링크; main 머지 후 Pages 소스를 main으로 전환 필요

- [x] 우표첩 다중 선택 삭제 — '선택' 모드: 탭으로 체크(금색 배지, 미선택 흐림), 하단 머티리얼 바에 n장 카운트+삭제, 확인 다이얼로그 후 일괄 삭제

## 확인 필요 (실기기)
- [ ] 라이브 우표: 메시지 스티커 서랍에서 실제로 움직이는지 + 대화에 붙였을 때 애니메이션 확인
- [ ] 실제 기기에서 촬영 시 창 위치/크롭 정합성 미세 조정 (teethX/Y, 윈도우 rect)
- [ ] 전면 카메라 미러링 결과 확인

## 향후 아이디어
- [ ] 우표 종이 질감/세피아 필터
- [ ] 컬렉션 항목 삭제·공유
- [ ] 우표 아래 날짜 캡션
